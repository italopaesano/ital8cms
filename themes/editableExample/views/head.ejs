<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <%- passData.pluginSys.hookPage("head", passData); %>

    <%# Default Bootstrap CSS from plugin %>
    <link rel="stylesheet" href="/<%- passData.apiPrefix %>/bootstrap/css/bootstrap.min.css">

    <%# Custom theme styles %>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        }
        .hero {
            padding: 4rem 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
        .content-section {
            padding: 3rem 0;
        }
        footer {
            background: #f8f9fa;
            padding: 2rem 0;
            margin-top: 3rem;
        }
    </style>
</head>
